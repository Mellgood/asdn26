# TODO: Start the OpenVSwitch daemon
# ...

# TODO: Create an OVS bridge named "s3"
# ...

# TODO: Add eth0 and eth1 to the bridge (data ports only — do NOT add eth2)
# ...

# Management interface — gives s3 IP connectivity to the controller
# This interface is NOT part of the OVS bridge (it's for control-plane only)
ip addr add 172.16.0.30/24 dev eth2

# Connect to the Ryu controller on the management network
ovs-vsctl set-controller s3 tcp:172.16.0.1:6653
