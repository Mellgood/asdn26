# FRRouting OSPF Configuration for r2
# Following the pattern from r1/etc/frr/frr.conf, configure OSPF.
# r2's connected networks: 10.0.0.0/24 (eth0), 10.0.2.0/24 (eth1), 10.0.4.0/24 (eth2)

router ospf

# TODO: Advertise all three of r2's connected networks into area 0
# ...
