# FRRouting OSPF Configuration for r3
# Following the pattern from r1/etc/frr/frr.conf, configure OSPF.
# r3's connected networks: 10.0.1.0/24 (eth0), 10.0.2.0/24 (eth1), 10.0.5.0/24 (eth2)

router ospf

# TODO: Advertise all three of r3's connected networks into area 0
# ...
